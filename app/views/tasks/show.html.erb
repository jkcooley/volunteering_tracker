<h1><%= @org_profile.name %></h1>

<h2><%= @task.title %></h2>
<%= @task.blurb %>

<!-- TODO if the user is logged in, pass info to this --!>
<% if @user_signed_in %>
  <p>
    <a href="/tasks/<%= @task_id %>/users/<%= @user_id %>">Log this task</a>
  </p>
  <% else %>
  <p>
  Sign in to log this task
  </p>
  <% end %>

<style>
body {
  font-family: 'Courier New', monospace;
  background-color:#A0BFB6
}
</style>